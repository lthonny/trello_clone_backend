const { expect } = require('chai');
const userService = require('../app/services/auth/userServices');
const userController = require('../app/contrellers/userController');

describe('User service', function() {
  beforeEach(async function() {});
  afterEach(async function() {});

  it('should isauth', async function() {
    // const isauth = await userController.isauth();
    // console.log(isauth);
    // expect(assigned.exist).to.equal('user has already been added');
  });
});